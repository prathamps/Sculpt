-- Sculpt is fully open source: every feature is available to every user.
-- Drop the subscription/plan machinery left over from the paid product.
DROP TABLE IF EXISTS "Subscription";

DROP TYPE IF EXISTS "Plan";
